Menu="Dashboard:0"
Icon="PlexStreams-icon.png"
Tag="film"
Cond="parse_ini_file('/boot/config/plugins/plexstreamsplus/plexstreamsplus.cfg')['DISPLAY_WIDGET'] == '1' && version_compare(parse_ini_file('/etc/unraid-version')['version'],'6.12.0-beta5', '>')"
---
<style>
#plexstreamsplus_streams p.plexstream-title {
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
    width: 200px;
    padding: 0 0 0 0;
    margin: 0 0 10px 0;
}

#plexstreamsplus_streams p.plexstream-user {
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
    width: 104px;
    padding: 0 0 0 0;
    margin: 0 0 10px 0;
}

#plexstreamsplus_streams p.plexstream-time {
    padding: 0 0 0 0;
    margin: 0 0 10px 0;
    font-size:10px;
    width: 100%;
    white-space: nowrap;
    overflow: visible;
    text-overflow: clip;
    text-align: right;
}

td.plexstreamsplus-widget-header {
    position: relative;
    padding-right: 64px;
}

.plexstreamsplus-widget-header .section {
    line-height: 1.55rem !important;
}

.plexstreamsplus-widget-actions {
    position: absolute;
    top: 2px;
    right: 0;
    display: inline-flex;
    align-items: flex-start;
    gap: 2px;
}

.plexstreamsplus-time-col {
    overflow: visible !important;
}
</style>
<?PHP
    /*
        Create a .page file based on this example file and store the file in your plugin directory
        Make sure this file has a unique name not used by the GUI or other plugins

        This is an example configuration which is used to add custom tiles to the dashboard
        Placement of one or more custom tiles is done under column1, column2 or column3
        A column can have as many new custom tiles as needed, each custom tile is a <tbody> element
        Each tile must have these two properties set:

        "_(description)_"    - this is the (translated) description shown in the Content Manager window
        "_(Tile tile)_"      - this is the (translated) title of the tile shown in bold on the screen

        The content of the tile can be any text as required by your plugin, this content is placed inside the <td> element
        A number of predefined spans are available to make fields of certain widths and maybe used to follow the GUI alignment
        These are:
        <span class="w18">
        <span class="w26">
        <span class="w36">
        <span class="w44">
        <span class="w72">
    */
    $plugin = "plexstreamsplus";
    $translations = file_exists("$docroot/webGui/include/Translations.php");
    $dashboard_widget_pos = 1;
    $dashboard_widget_colspan = 3;
    $streamCount = 0;
?>

<style type="text/css">
        .dash_plexstreamsplus_toggle{display:none}
        .dash_plexstreamsplus_toggle tbody td {
            padding-top: 0px;
            padding-bottom: 0px;
        }
        #plexstreamsplus_streams p.plexstream-title {
            white-space: nowrap;
            overflow: hidden;
            text-overflow: ellipsis;
            width: 200px;
            padding: 0 0 0 0;
            margin: 0 0 10px 0;
        }

        #plexstreamsplus_streams p.plexstream-user {
            white-space: nowrap;
            overflow: hidden;
            text-overflow: ellipsis;
            width: 104px;
            padding: 0 0 0 0;
            margin: 0 0 10px 0;
        }

        #plexstreamsplus_streams p.plexstream-time {
            padding: 0 0 0 0;
            margin: 0 0 10px 0;
            font-size:10px;
            width: 100%;
            white-space: nowrap;
            overflow: visible;
            text-overflow: clip;
            text-align: right;
        }

        td.plexstreamsplus-widget-header {
            position: relative;
            padding-right: 64px;
        }

        .plexstreamsplus-widget-header .section {
            line-height: 1.55rem !important;
        }

        .plexstreamsplus-widget-actions {
            position: absolute;
            top: 2px;
            right: 0;
            display: inline-flex;
            align-items: flex-start;
            gap: 2px;
        }

        .plexstreamsplus-time-col {
            overflow: visible !important;
        }
</style>
<?php
$mytiles[$plugin]['column1'] =
<<<EOT
<tbody class="sortable" title="_(PlexStreams Plus)_">
    <tr>
        <td class="plexstreamsplus-widget-header">
            <i style="font-size:32px;vertical-align:top;" class="fa fa-film icon"></i>
            <div class="section" style="line-height:2rem;">_(PlexStreams Plus)_<br><span id="stream_count_container"><span id="plexstreamsplus_count">0</span> _(Active Stream(s))_</span></div>
            <span class="plexstreamsplus-widget-actions">
                <a href="/Settings/PlexStreamsPlus" title="_(PlexStreams Plus Setup page)_"><i class="fa fa-fw fa-cog control"></i></a>
                <a href="/Tools/PlexStreamsPlusTools" title="_(View Streams)_"><i class="fa fa-fw fa-film control"></i></a>
            </span>
        </td>
    </tr>
    <tr>
        <td>            
            <span class="w36">_(Name)_</span>
            <span class="w18" align="center">_(Status)_</span>
            <span class="w18">_(User)_</span>
            <span class="w26 plexstreamsplus-time-col" align="right">_(Time)_</span>
        </td>
    </tr>
    <tr>
        <td>
            <div id="plexstreamsplus_streams">
                <div id="retrieving_streams">
                    <p align="center" style="font-style:italic;text-align:center;">_(Retrieving Stream Data...)_</p>
                </div>
            </div>
        </td>
    </tr>
</tbody>
EOT;
?>
<script src="<?autov('/plugins/plexstreamsplus/js/plex.js')?>"></script>
<script async>
    $(function() {
        plexStreamsPlusStartPolling('dashboard_new', function() {
            return updateDashboardStreamsNew('dashboard_new');
        });
    });
</script>

