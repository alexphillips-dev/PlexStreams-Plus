Menu="PlexStreamsPlus"
Title="PlexStreams Plus Settings"
Tag="film"
Icon="PlexStreams-icon.png"
---
<link type="text/css" rel="stylesheet" href="/plugins/plexstreamsplus/spinner.css">
<?php
include('/usr/local/emhttp/plugins/plexstreamsplus/includes/config.php');
include('/usr/local/emhttp/plugins/plexstreamsplus/includes/common.php');

if (!isset($cfg['DISPLAY_WIDGET'])) {
    $cfg['DISPLAY_WIDGET'] = "1";
}

if (!isset($cfg['DISPLAY_NAV'])) {
    $cfg['DISPLAY_NAV'] = "1";
}

if (!isset($cfg['FORCE_PLEX_HTTPS'])) {
    $cfg['FORCE_PLEX_HTTPS'] = "0";
}

if (!isset($cfg['CUSTOM_SERVERS'])) {
    $cfg['CUSTOM_SERVERS'] = "";
}

if (!isset($cfg['HOST'])) {
    $cfg['HOST'] = "";
}

if (!isset($cfg['TOKEN'])) {
    $cfg['TOKEN'] = "";
}

$pluginPath = '/usr/local/emhttp/plugins/plexstreamsplus/';

if ($cfg['DISPLAY_NAV'] === '0' && file_exists($pluginPath . 'PlexStreamsPlusNav.page')) {
    rename($pluginPath .'PlexStreamsPlusNav.page', $pluginPath .'PlexStreamsPlusNav.page.off');
} else if ($cfg['DISPLAY_NAV'] === '1' && file_exists($pluginPath . 'PlexStreamsPlusNav.page.off')) {
    rename($pluginPath .'PlexStreamsPlusNav.page.off', $pluginPath .'PlexStreamsPlusNav.page');
}

if ($cfg['DISPLAY_WIDGET'] === '0' && file_exists($pluginPath . 'PlexStreamsPlus_dashboard.page')) {
    rename($pluginPath .'PlexStreamsPlus_dashboard.page', $pluginPath .'PlexStreamsPlus_dashboard.page.off');
} else if ($cfg['DISPLAY_WIDGET'] === '1' && file_exists($pluginPath . 'PlexStreamsPlus_dashboard.page.off')) {
    rename($pluginPath .'PlexStreamsPlus_dashboard.page.off', $pluginPath .'PlexStreamsPlus_dashboard.page');
}
?>

<script>
function clearToken() {
    document.getElementById('plex-token').value = '';
    if (typeof plexStreamsPlusSetTokenState === 'function') {
        plexStreamsPlusSetTokenState('disconnected');
    }
    document.getElementById('plexstreamsplus_settings').submit();
}
</script>
<style>
    .psplus-settings-shell {
        width: min(1200px, 100%);
        margin: 14px auto 4px;
        display: flex;
        flex-direction: column;
        gap: 14px;
    }

    .psplus-settings-hero {
        display: flex;
        align-items: center;
        justify-content: space-between;
        gap: 14px;
        padding: 14px 16px;
        border: 1px solid #2a333f;
        background: linear-gradient(135deg, rgba(24, 33, 44, 0.92), rgba(12, 18, 25, 0.96));
    }

    .psplus-hero-title {
        margin: 0;
        font-size: 20px;
        font-weight: 700;
        line-height: 1.2;
        color: #f1f5fb;
    }

    .psplus-hero-subtitle {
        margin: 4px 0 0;
        font-size: 13px;
        color: #b6c3d5;
    }

    .psplus-version-pill {
        border: 1px solid #39506a;
        background: rgba(13, 28, 44, 0.72);
        color: #d6e7fb;
        padding: 5px 10px;
        font-size: 11px;
        letter-spacing: 0.06em;
        text-transform: uppercase;
        font-weight: 700;
        white-space: nowrap;
    }

    .psplus-settings-form {
        display: flex;
        flex-direction: column;
        gap: 14px;
    }

    .psplus-settings-grid {
        display: grid;
        grid-template-columns: repeat(2, minmax(300px, 1fr));
        gap: 14px;
    }

    .psplus-card {
        border: 1px solid #2a333f;
        background: linear-gradient(180deg, rgba(21, 28, 37, 0.92), rgba(11, 17, 24, 0.96));
        padding: 14px 16px 15px;
        display: flex;
        flex-direction: column;
        gap: 10px;
        min-height: 0;
    }

    .psplus-card h2 {
        margin: 0;
        font-size: 15px;
        font-weight: 700;
        letter-spacing: 0.02em;
        color: #f4f8fd;
    }

    .psplus-card p {
        margin: 0;
        color: #9fb0c4;
        font-size: 12px;
        line-height: 1.35;
    }

    .psplus-token-row {
        display: grid;
        grid-template-columns: auto 1fr;
        gap: 10px;
        align-items: center;
    }

    .psplus-btn {
        border: 1px solid #2f445a;
        background: #132231;
        color: #e9f2ff;
        font-size: 12px;
        font-weight: 700;
        letter-spacing: 0.05em;
        text-transform: uppercase;
        padding: 9px 12px;
        line-height: 1;
        cursor: pointer;
    }

    .psplus-btn:hover {
        background: #1a2f45;
    }

    .psplus-btn:disabled {
        opacity: 0.55;
        cursor: default;
    }

    .psplus-btn-primary {
        border-color: #376a9b;
        background: linear-gradient(135deg, #245f97, #1f4a7c);
    }

    .psplus-btn-primary:hover {
        background: linear-gradient(135deg, #2b6ca9, #25588f);
    }

    .psplus-btn-danger {
        border-color: #70414a;
        background: #2f1b22;
        color: #ffd4db;
    }

    .psplus-btn-danger:hover {
        background: #3e232c;
    }

    .psplus-btn-secondary {
        border-color: #3f546a;
        background: #1a2938;
        color: #d8e6f5;
    }

    .psplus-btn-secondary:hover {
        background: #213447;
    }

    .psplus-input {
        width: 100%;
        min-height: 37px;
        border: 1px solid #344556;
        background: #101922;
        color: #f3f8ff;
        padding: 8px 10px;
        font-size: 13px;
    }

    .psplus-input:focus {
        border-color: #4d87bb;
        outline: none;
        box-shadow: 0 0 0 1px rgba(81, 139, 194, 0.35);
    }

    .psplus-input-invalid {
        border-color: #8b4756;
        box-shadow: 0 0 0 1px rgba(139, 71, 86, 0.45);
    }

    .psplus-card-head {
        display: flex;
        align-items: center;
        justify-content: space-between;
        gap: 8px;
    }

    .psplus-token-meta {
        display: flex;
        align-items: center;
        flex-wrap: wrap;
        gap: 8px;
    }

    .psplus-state-pill {
        border: 1px solid #3d546b;
        background: rgba(16, 30, 45, 0.74);
        color: #d2e4f8;
        font-size: 10px;
        text-transform: uppercase;
        letter-spacing: 0.06em;
        font-weight: 700;
        padding: 4px 8px;
    }

    .psplus-state-pill.is-connected {
        border-color: #2f6a4c;
        background: rgba(21, 55, 39, 0.75);
        color: #d4ffe8;
    }

    .psplus-state-pill.is-loading {
        border-color: #536a85;
        color: #d8e7f7;
    }

    .psplus-state-pill.is-error {
        border-color: #7f4c58;
        background: rgba(58, 31, 38, 0.8);
        color: #ffd4dc;
    }

    .psplus-meta-note {
        font-size: 11px;
        color: #8fa4ba;
    }

    .psplus-server-head-actions {
        display: inline-flex;
        align-items: center;
        gap: 8px;
        flex-wrap: wrap;
    }

    .psplus-selection-chip {
        border: 1px solid #38506a;
        background: rgba(17, 32, 50, 0.72);
        color: #c7d8ec;
        font-size: 11px;
        font-weight: 700;
        text-transform: uppercase;
        letter-spacing: 0.06em;
        padding: 4px 8px;
        white-space: nowrap;
    }

    .psplus-selection-chip strong {
        color: #ffffff;
        margin-left: 4px;
    }

    .psplus-server-status {
        border: 1px solid #30455a;
        background: rgba(15, 26, 39, 0.78);
        color: #bad0e7;
        font-size: 11px;
        padding: 6px 8px;
        line-height: 1.2;
    }

    .psplus-server-status.is-success {
        border-color: #2f644a;
        color: #c9f0de;
        background: rgba(21, 49, 37, 0.7);
    }

    .psplus-server-status.is-warning {
        border-color: #6e6037;
        color: #f4e3b2;
        background: rgba(52, 44, 20, 0.7);
    }

    .psplus-server-status.is-error {
        border-color: #784856;
        color: #ffd6de;
        background: rgba(56, 27, 35, 0.7);
    }

    .psplus-server-load {
        min-height: 22px;
        display: flex;
        align-items: center;
    }

    .psplus-server-load .lds-dual-ring {
        position: static;
        width: 26px;
        height: 26px;
        padding-right: 0;
        display: none;
    }

    .psplus-server-load .lds-dual-ring:after {
        width: 16px;
        height: 16px;
        margin: 4px;
        border-width: 3px;
        border-color: #72a8da transparent #72a8da transparent;
    }

    .psplus-server-list {
        border: 1px solid #2f3c4d;
        background: rgba(9, 14, 20, 0.7);
        padding: 8px;
        max-height: 255px;
        overflow-y: auto;
    }

    .psplus-server-item {
        margin-bottom: 7px;
    }

    .psplus-server-item:last-child {
        margin-bottom: 0;
    }

    .psplus-server-item > input[type='checkbox'] {
        position: absolute;
        opacity: 0;
        width: 0;
        height: 0;
    }

    .psplus-server-label {
        border: 1px solid #2a3441;
        background: #101821;
        color: #d9e3f0;
        display: flex;
        align-items: center;
        justify-content: space-between;
        gap: 10px;
        padding: 8px 9px;
        cursor: pointer;
    }

    .psplus-server-item > input[type='checkbox']:checked + .psplus-server-label {
        border-color: #4f84b5;
        background: linear-gradient(135deg, rgba(25, 50, 74, 0.95), rgba(16, 30, 44, 0.95));
    }

    .psplus-server-main {
        min-width: 0;
        display: flex;
        flex-direction: column;
        gap: 2px;
    }

    .psplus-server-name {
        font-size: 13px;
        font-weight: 700;
        color: #f5f9ff;
        text-overflow: ellipsis;
        overflow: hidden;
        white-space: nowrap;
    }

    .psplus-server-address {
        font-size: 11px;
        color: #9eb2c8;
        text-overflow: ellipsis;
        overflow: hidden;
        white-space: nowrap;
    }

    .psplus-server-scope {
        font-size: 10px;
        font-weight: 700;
        letter-spacing: 0.06em;
        text-transform: uppercase;
        border: 1px solid #3d566f;
        padding: 3px 7px;
        color: #c6daee;
        white-space: nowrap;
    }

    .psplus-server-scope.is-remote {
        border-color: #7f4f59;
        color: #ffc8d1;
        background: rgba(58, 29, 37, 0.35);
    }

    .psplus-server-empty {
        margin: 0;
        color: #d5a9ae;
        font-size: 12px;
        font-style: italic;
    }

    .psplus-input-feedback {
        font-size: 11px;
        color: #90a6be;
        min-height: 15px;
    }

    .psplus-input-feedback.is-success {
        color: #c7efd8;
    }

    .psplus-input-feedback.is-error {
        color: #ffc8d3;
    }

    .psplus-setting-stack {
        display: flex;
        flex-direction: column;
        gap: 10px;
    }

    .psplus-setting-row {
        border: 1px solid #2a3441;
        background: rgba(14, 20, 29, 0.82);
        padding: 10px;
        display: flex;
        align-items: center;
        justify-content: space-between;
        gap: 10px;
        flex-wrap: wrap;
    }

    .psplus-setting-title {
        color: #f2f7fd;
        font-size: 13px;
        font-weight: 700;
    }

    .psplus-setting-help {
        color: #8ea0b6;
        font-size: 11px;
        margin-top: 2px;
    }

    .psplus-segment {
        display: inline-flex;
        border: 1px solid #2f455c;
        background: #101b28;
        overflow: hidden;
    }

    .psplus-segment input[type='radio'] {
        position: absolute;
        opacity: 0;
        width: 0;
        height: 0;
    }

    .psplus-segment label {
        min-width: 44px;
        text-align: center;
        padding: 7px 10px;
        font-size: 11px;
        text-transform: uppercase;
        letter-spacing: 0.05em;
        font-weight: 700;
        color: #9eb2c7;
        cursor: pointer;
        border-right: 1px solid #2f455c;
        line-height: 1;
    }

    .psplus-segment label:last-of-type {
        border-right: 0;
    }

    .psplus-segment input[type='radio']:checked + label {
        background: linear-gradient(135deg, #2a669a, #214c7c);
        color: #ffffff;
    }

    .psplus-actions {
        border: 1px solid #2a333f;
        background: rgba(14, 20, 28, 0.92);
        padding: 10px 12px;
        display: flex;
        justify-content: flex-end;
        gap: 8px;
    }

    @media (max-width: 980px) {
        .psplus-settings-grid {
            grid-template-columns: 1fr;
        }

        .psplus-token-row {
            grid-template-columns: 1fr;
        }

        .psplus-server-head-actions {
            width: 100%;
            justify-content: space-between;
        }

        .psplus-actions {
            justify-content: stretch;
            flex-direction: column;
        }

        .psplus-actions .psplus-btn {
            width: 100%;
        }
    }
</style>
<div class="psplus-settings-shell">
    <div class="psplus-settings-hero">
        <div>
            <h1 class="psplus-hero-title">PlexStreams Plus Settings</h1>
            <p class="psplus-hero-subtitle">Connect Plex, choose servers, and control where PlexStreams Plus appears in Unraid.</p>
        </div>
        <div class="psplus-version-pill">Version <?php echo(htmlspecialchars(PLUGIN_VERSION, ENT_QUOTES)); ?></div>
    </div>

    <form name="plexstreamsplus_settings" id="plexstreamsplus_settings" method="POST" action="/update.php" target="progressFrame" class="psplus-settings-form">
        <input type="hidden" name="#file" value="plexstreamsplus/plexstreamsplus.cfg" />
        <input type="hidden" id="COMMAND" name="#command" value="" />
        <input type="hidden" id="HOST" name="HOST" value="<?php echo(htmlspecialchars($cfg['HOST'], ENT_QUOTES)); ?>" />

        <div class="psplus-settings-grid">
            <section class="psplus-card">
                <h2>Authentication</h2>
                <p>Authorize this plugin with Plex to load your available servers.</p>
                <div class="psplus-token-meta">
                    <span id="psplus-token-status" class="psplus-state-pill is-disconnected">Not Connected</span>
                    <span id="psplus-token-note" class="psplus-meta-note">Get a Plex token to discover available servers.</span>
                </div>
                <div class="psplus-token-row">
                    <button type="button" id="psplus-get-token-btn" class="psplus-btn psplus-btn-primary" onclick="plexStreamsPlusStartOAuth()">Get Plex Token</button>
                    <input type="text" class="psplus-input" id="plex-token" name="TOKEN" value="<?php echo(htmlspecialchars($cfg['TOKEN'], ENT_QUOTES)) ?>" readonly="true" />
                </div>
                <p>Token is stored locally in your Unraid plugin configuration.</p>
            </section>

            <section class="psplus-card">
                <div class="psplus-card-head">
                    <h2>Servers</h2>
                    <div class="psplus-server-head-actions">
                        <button type="button" class="psplus-btn psplus-btn-secondary" id="psplus-refresh-servers">Refresh</button>
                        <div class="psplus-selection-chip">Selected <strong id="psplus-selected-count">0</strong></div>
                    </div>
                </div>
                <p>Select which Plex servers should be monitored.</p>
                <div class="psplus-server-status is-info" id="psplus-server-status">Ready to load Plex servers.</div>
                <div class="psplus-server-load"><div class="lds-dual-ring"></div></div>
                <div id="hostcontainer" class="psplus-server-list"></div>
            </section>

            <section class="psplus-card">
                <h2>Custom Servers</h2>
                <p>Add manual Plex endpoints if discovery does not return all servers.</p>
                <input type="text" class="psplus-input" name="CUSTOM_SERVERS" id="CUSTOM_SERVERS" placeholder="Example: 192.168.1.2:32400" value="<?php echo(htmlspecialchars($cfg['CUSTOM_SERVERS'], ENT_QUOTES)); ?>" />
                <p id="psplus-custom-servers-feedback" class="psplus-input-feedback is-info">Optional: add comma-separated host:port endpoints.</p>
            </section>

            <section class="psplus-card">
                <h2>Behavior</h2>
                <div class="psplus-setting-stack">
                    <div class="psplus-setting-row">
                        <div>
                            <div class="psplus-setting-title">Display in Navigation</div>
                            <div class="psplus-setting-help">Show Plex Streams in the top navigation bar.</div>
                        </div>
                        <div class="psplus-segment">
                            <input type="radio" name="DISPLAY_NAV" id="DISPLAY_NAV_ON" value="1" <?php echo($cfg['DISPLAY_NAV'] === "1" ? "checked" : ""); ?> />
                            <label for="DISPLAY_NAV_ON">On</label>
                            <input type="radio" name="DISPLAY_NAV" id="DISPLAY_NAV_OFF" value="0" <?php echo($cfg['DISPLAY_NAV'] === "0" ? "checked" : ""); ?> />
                            <label for="DISPLAY_NAV_OFF">Off</label>
                        </div>
                    </div>

                    <div class="psplus-setting-row">
                        <div>
                            <div class="psplus-setting-title">Display Dashboard Widget</div>
                            <div class="psplus-setting-help">Show active streams on the main Unraid dashboard.</div>
                        </div>
                        <div class="psplus-segment">
                            <input type="radio" name="DISPLAY_WIDGET" id="DISPLAY_WIDGET_ON" value="1" <?php echo($cfg['DISPLAY_WIDGET'] === "1" ? "checked" : ""); ?> />
                            <label for="DISPLAY_WIDGET_ON">On</label>
                            <input type="radio" name="DISPLAY_WIDGET" id="DISPLAY_WIDGET_OFF" value="0" <?php echo($cfg['DISPLAY_WIDGET'] === "0" ? "checked" : ""); ?> />
                            <label for="DISPLAY_WIDGET_OFF">Off</label>
                        </div>
                    </div>

                    <div class="psplus-setting-row">
                        <div>
                            <div class="psplus-setting-title">Use SSL Connection</div>
                            <div class="psplus-setting-help">Prefer HTTPS when discovering and querying Plex servers.</div>
                        </div>
                        <div class="psplus-segment">
                            <input type="radio" name="FORCE_PLEX_HTTPS" id="FORCE_PLEX_HTTPS_ON" value="1" onchange="getServers('#hostcontainer', document.getElementById('HOST').value)" <?php echo($cfg['FORCE_PLEX_HTTPS'] === "1" ? "checked" : ""); ?> />
                            <label for="FORCE_PLEX_HTTPS_ON">On</label>
                            <input type="radio" name="FORCE_PLEX_HTTPS" id="FORCE_PLEX_HTTPS_OFF" value="0" onchange="getServers('#hostcontainer', document.getElementById('HOST').value)" <?php echo($cfg['FORCE_PLEX_HTTPS'] === "0" ? "checked" : ""); ?> />
                            <label for="FORCE_PLEX_HTTPS_OFF">Off</label>
                        </div>
                    </div>
                </div>
            </section>
        </div>

        <div class="psplus-actions">
            <button type="button" class="psplus-btn psplus-btn-danger" onclick="clearToken()">Remove Token</button>
            <button type="submit" id="psplus-save-btn" class="psplus-btn psplus-btn-primary">Save Changes</button>
        </div>
    </form>
</div>
<script>
    var OS_VERSION = '<?php echo($GLOBALS['unRaidSettings']['version']); ?>';
    var PLUGIN_VERSION = '<?php echo(PLUGIN_VERSION); ?>';
</script>
<script src="<?autov('/plugins/plexstreamsplus/js/plex.js')?>"></script>
<script>
$(function() {
    plexStreamsPlusInitSettingsPage();
    getServers('#hostcontainer', $('#HOST').val());
});
</script>
